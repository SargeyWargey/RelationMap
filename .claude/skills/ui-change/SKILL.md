# UI Change Skill
1. First, grep/glob to find ALL files containing the component or style being changed
2. List every affected file to the user before editing
3. Use simple pixel-based CSS values, not complex calculations
4. After edits, verify no variable references were broken by the change
5. Confirm changes appear on all relevant pages/routes
